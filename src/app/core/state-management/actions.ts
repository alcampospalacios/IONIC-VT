/* eslint-disable @typescript-eslint/naming-convention */
export enum ActionTypes {
  CREATE_OBJECT = '[ACP] Create a object',
  DELETE_OBJECT = '[ACP] Delete a object',
  GET_OBJECT = '[ACP] Get all object',
}
